interface:
  display_name: "Forge"
  short_description: "Repo-specific patterns and workflows for forge"
  default_prompt: "Use the forge repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true