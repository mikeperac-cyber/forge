# Normalize everything to LF in the repo; let each checkout use its own
# native line endings. Stops "every file changed" diffs from Windows/Unix
# checkouts disagreeing about CRLF vs LF.
* text=auto eol=lf

*.png binary
*.jpg binary
*.jpeg binary
*.ico binary
*.woff binary
*.woff2 binary
